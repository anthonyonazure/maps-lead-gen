{
  "name": "maps-lead-gen",
  "version": "1.0.1",
  "private": true,
  "description": "Desktop lead generation app for finding, scoring, and exporting Google Maps business leads.",
  "author": "Anthony Clendenen",
  "license": "MIT",
  "main": "electron/main.cjs",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "bun test",
    "mutation": "stryker run",
    "check": "bun run typecheck && bun run lint",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w server && npm run bundle:server && npm run build -w client",
    "bundle:server": "node scripts/bundle-server.mjs",
    "electron:dev": "npm run build && electron .",
    "electron:build": "npm run build && electron-builder --publish never",
    "electron:build:win": "npm run build && electron-builder --win --publish never",
    "electron:build:mac": "npm run build && electron-builder --mac --publish never",
    "dist:win": "npm run electron:build:win"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stryker-mutator/core": "^9.6.1",
    "@types/bun": "^1.3.14",
    "concurrently": "^9.1.0",
    "electron": "^39.8.10",
    "electron-builder": "^26.0.0",
    "eslint": "^10.8.1",
    "typescript-eslint": "^8.66.0"
  },
  "overrides": {
    "@babel/core": "^7.29.6",
    "@xmldom/xmldom": "^0.8.13",
    "body-parser": "^2.3.0",
    "brace-expansion@^1.0.0": "^1.1.18",
    "brace-expansion@^2.0.0": "^2.1.4",
    "brace-expansion@^5.0.0": "^5.0.9",
    "esbuild": "^0.28.1",
    "fast-uri": "^3.1.5",
    "form-data": "^4.0.6",
    "ip-address": "^10.3.1",
    "js-yaml": "^4.3.1",
    "path-to-regexp": "^8.4.0",
    "picomatch": "^4.0.4",
    "postcss": "^8.5.23",
    "qs": "^6.15.2",
    "shell-quote": "^1.9.0",
    "tar": "^7.5.21",
    "tmp": "^0.2.6"
  }
}
